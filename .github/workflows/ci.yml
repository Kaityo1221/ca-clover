name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npm run build
      - name: Diagnostic Bukuro creator
        shell: bash
        run: |
          set -euo pipefail
          ID='0faf7b6f-545b-4638-8c55-09a8142039f5'
          BODY="$(jq -nc --arg id "$ID" '{query:"query Probe($id: ID!) { event(id: $id) { id name clubId createdByCommunityAmbassador creator { id displayName badges { badgeType alias } } } }",variables:{id:$id}}')"
          RESP="$(curl -sS -X POST 'https://niantic-social-api.nianticlabs.com/graphql' -H 'Content-Type: application/json' -H 'Accept: application/json' --data "$BODY")"
          echo "$RESP" | jq '{errors, event: .data.event}'
