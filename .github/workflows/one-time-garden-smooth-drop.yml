name: One-time Clover Garden smooth drop

on:
  push:
    branches: [pages]
    paths:
      - .github/workflows/one-time-garden-smooth-drop.yml

permissions:
  contents: write

jobs:
  patch-garden-smooth-drop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: pages
          fetch-depth: 0

      - name: Apply Garden smooth drop patch
        run: python scripts/one_time_garden_smooth_drop.py

      - name: Verify JavaScript syntax
        run: node --check docs/clover-garden.js

      - name: Verify cache key
        run: grep -q 'garden-smooth1' docs/index.html

      - name: Commit patch
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/clover-garden.js docs/index.html
          git commit -m "fix: smooth Clover Garden drop on iPhone Safari"
          git push origin HEAD:pages
