name: Patch rare four-leaf Easter egg

on:
  push:
    branches: [pages]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: pages
          fetch-depth: 0
      - name: Apply rare four-leaf Easter egg
        run: python scripts/patch-clover-lucky-easter-egg.py
      - name: Syntax check
        run: node --check docs/clover-garden.js
      - name: Commit patch
        run: |
          if git diff --quiet -- docs/clover-garden.js docs/index.html; then
            echo "No patch needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/clover-garden.js docs/index.html
          git commit -m "feat: add rare four-leaf Garden Easter egg"
          git push origin HEAD:pages
